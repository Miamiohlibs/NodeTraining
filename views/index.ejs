<html lang="en">
    <%- include('partials/head') %>
    <body>
        <h1><%= title %></h1>
        <div class="container">
            <%- include('partials/form') %>
        </div>
        <div class="container">
            <% if (typeof location != "undefined") { %>
                <%= (location) %>
            <% } %>
        </div>
    </body>
</html>